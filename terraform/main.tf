# Remove the module block - you're already in the root module
# module "artifact" {
#   source = "./"
# }

# Add the actual resources or import them from other files
# (Your resources are already defined in separate files)
